/**
 * Wikibooks pages -> data/grammar/<topic>.md.
 *
 * Phase 2 of the build plan. Not implemented yet — this stub exists so the
 * pipeline's shape is fixed and scripts/validate-data.ts has something to
 * validate against.
 *
 * Writes a YAML front-matter attribution header (source, sourceUrl, license,
 * modified) on every topic, as CC BY-SA requires.
 * */
console.error(
  'build-grammar.ts is not implemented yet (phase 2: data pipeline).\n' +
    'See README.md "Data pipeline" for what it will do.',
);
process.exit(1);
