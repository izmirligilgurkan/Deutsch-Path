/**
 * kaikki JSONL -> data/lexicon.json.
 *
 * Phase 2 of the build plan. Not implemented yet — this stub exists so the
 * pipeline's shape is fixed and scripts/validate-data.ts has something to
 * validate against.
 *
 * Keeps lemma, pos, gender, plural, form tables, glosses and provenance.
 * Filters to the course vocabulary so data/ stays inside the size budget.
 * */
console.error(
  'build-lexicon.ts is not implemented yet (phase 2: data pipeline).\n' +
    'See README.md "Data pipeline" for what it will do.',
);
process.exit(1);
