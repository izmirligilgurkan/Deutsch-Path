node_modules/
dist/
dist-ssr/
*.local

# Raw source dumps: large, and re-fetchable from their dated manifest.
raw/

# The source PDFs stay out of the repository; the extracted level list in
# data/goethe-levels.json is committed deliberately — see DATA_LICENSES.md.
*Wortliste*.pdf
*wortliste*.pdf
# A list a learner extracts for themselves, at the repository root.
/goethe-levels.json

# Test output
playwright-report/
test-results/
coverage/

.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.log
